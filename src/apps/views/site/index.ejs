
<%- include("./layouts/head.ejs")%>
    <title><%=thongtintrang.title%></title>
    <meta name="description" content="<%=thongtintrang.description%>" />
    <meta name="keywords" content="<%=thongtintrang.keywords%>" />
    <meta property="og:title" content="<%=thongtintrang.title%>" />
    <meta property="og:description" content="<%=thongtintrang.description%>" />
    <meta property="og:keywords" content="<%=thongtintrang.keywords%>" />
   

</head>

<body class="home blog theme-mtw-haiau woocommerce-no-js"><noscript><iframe
            src="https://www.googletagmanager.com/ns.html?id=GTM-WPQ49BL" height="0" width="0"
            style="display:none;visibility:hidden"></iframe></noscript>
 <%- include("./layouts/menu.ejs")%>


    <div class="container">

<div class="slider row">
  <div class="slides" id="slides">
       <%for(item of banner){%>
    <img src="./images/update/<%=item.images%>" alt="<%=item.images%>">
<%}%>
  </div>

<button class="prev" onclick="moveSlide(-1)">&#10094;</button>
<button class="next" onclick="moveSlide(1)">&#10095;</button>


  <!-- Dấu chấm -->
  <div class="dots" id="dots"></div>
</div>
</div> </br>
<style>
.slider {
  position: relative;
  overflow: hidden;
  max-width: 100%;
}

.slides {
  display: flex;
  transition: transform 0.6s ease-in-out;
}
.slides img {
  width: 100%;
  flex-shrink: 0;
}

/* Nút mũi tên */
.prev, .next {
  cursor: pointer;
  position: absolute;
  top: 50%;
  padding: 12px;
  margin-top: -22px;
  color: white;
  font-size: 30px;
  font-weight: bold;
  user-select: none;
  background: rgba(0,0,0,0.3);
  border-radius: 50%;
}
.next { right: 10px; }
.prev { left: 10px; }

/* Chấm */
.dots {
  text-align: center;
  position: absolute;
  bottom: 10px;
  width: 100%;
}
.dot {
  cursor: pointer;
  height: 12px;
  width: 12px;
  margin: 0 3px;
  background-color: #bbb;
  border-radius: 50%;
  display: inline-block;
}
.active {
  background-color: #717171;
}
</style>

<script>
let slideIndex = 0;
const slides = document.getElementById("slides");
const images = slides.getElementsByTagName("img");
const dotsContainer = document.getElementById("dots");

// tạo dot theo số ảnh
for (let i = 0; i < images.length; i++) {
  const dot = document.createElement("span");
  dot.classList.add("dot");
  dot.onclick = () => currentSlide(i);
  dotsContainer.appendChild(dot);
}

const dots = dotsContainer.getElementsByClassName("dot");

function showSlide(index) {
  if (index >= images.length) slideIndex = 0;
  else if (index < 0) slideIndex = images.length - 1;
  else slideIndex = index;

  slides.style.transform = `translateX(${-slideIndex * 100}%)`;

  // update dot active
  for (let i = 0; i < dots.length; i++) {
    dots[i].classList.remove("active");
  }
  dots[slideIndex].classList.add("active");
}

function moveSlide(step) {
  showSlide(slideIndex + step);
}

function currentSlide(index) {
  showSlide(index);
}

// auto chạy
setInterval(() => {
  showSlide(slideIndex + 1);
}, 3000);

// khởi tạo
showSlide(0);
</script>





    <div class="container mainpage white">
        <div class="row" id="homeproduct">
             <%for(item of product){%>
            <div id="tw-recent-posts-14" class="widget tw-recent-posts">
                <div class="tieude_home">
                    <h2 class="title_link"> <a class="title_link_a" title="PLC" href="/danh-muc/<%=item.product1.slug%>"> <span
                                class="glyphicon glyphicon-th-list"></span><%=item.product1.name%></a></h2>
                    <div class="link-cat"></div>
                </div>
                <div class="cont_home">
                         <%for(item2 of item.product2){%>
                    <div class="col-xs-6 col-md-2 col-sm-3 motsanpham">
                        <a href="/san-pham/<%=item2._id%>"
                            title="Module nhiệt độ Omron CP1W-TS104"><img width="400" height="400"
                                src="./images/update/<%=item2.images[0].name%>"
                                class="attachment-thumb-home-product size-thumb-home-product wp-post-image" alt=""
                           /></a>
                        <div class="caption">
                            <h3><a href="/san-pham/<%=item2._id%>"
                                    title="Module nhiệt độ Omron CP1W-TS104"><%=item2.name%></a></h3>
                  
<div class="post-price">
  <% if (item2.pricesale == 0) { %>
    <div class="price-contact">
   <span style="color: #333;">Giá:</span>  <span style=" color: #e60000;
  font-weight: bold">LIÊN HỆ</span> 
    </div>
  <% } else if (item2.pricesale && item2.pricesale < item2.price) { %>
    <div class="price-old">
      <%= new Intl.NumberFormat("vi-VN", {
            style: "currency",
            currency: "VND",
            minimumFractionDigits: 0,
        }).format(item2.price) %>
    </div>
    <div class="price-sale">
      <%= new Intl.NumberFormat("vi-VN", {
            style: "currency",
            currency: "VND",
            minimumFractionDigits: 0,
        }).format(item2.pricesale) %>
    </div>
    <div class="salepercent">
      -<%= Math.round((1 - item2.pricesale / item2.price) * 100) %>%
    </div>
  <% } else { %>
    <div class="price-normal">
      <%= new Intl.NumberFormat("vi-VN", {
            style: "currency",
            currency: "VND",
            minimumFractionDigits: 0,
        }).format(item2.price) %>
    </div>
  <% } %>
</div>







                        </div>
                    </div>
                    <%}%>
                </div>
            </div>
               <%}%>
        </div>
    </div>
    <style>
    .post-price {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 5px;
}

.price-old {
  color: #999;
  text-decoration: line-through;
  font-size: 14px;
}

.price-sale {
  color: #e53935;
  font-weight: bold;
  font-size: 18px;
}

.price-normal {
  color: #e53935;
  font-weight: bold;
  font-size: 18px;
}

.salepercent {
  background: #ff5722;
  color: #fff;
  font-size: 12px;
  font-weight: bold;
  padding: 2px 6px;
  border-radius: 4px;
}

</style>
    
              <%- include("./layouts/footer.ejs")%>